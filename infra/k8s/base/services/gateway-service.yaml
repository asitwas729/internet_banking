apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: internet-banking
spec:
  type: ClusterIP   # 외부 노출은 overlays/gcp/ingress.yaml 에서 처리
  selector:
    app: gateway-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
