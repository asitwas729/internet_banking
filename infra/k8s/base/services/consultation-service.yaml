apiVersion: v1
kind: Service
metadata:
  name: consultation-service
  namespace: internet-banking
spec:
  type: ClusterIP
  selector:
    app: consultation-service
  ports:
    - port: 8087
      targetPort: 8087
      name: http
