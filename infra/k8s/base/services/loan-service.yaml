apiVersion: v1
kind: Service
metadata:
  name: loan-service
  namespace: internet-banking
spec:
  type: ClusterIP
  selector:
    app: loan-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
