name: Deploy master-service

on:
  push:
    branches: [main]
    paths:
      - 'services/master-service/**'
      - 'common/**'
      - 'build.gradle'
      - 'settings.gradle'
      - '.github/workflows/deploy-master-service.yml'
      - '.github/workflows/reusable-*.yml'
  pull_request:
    branches: [main]
    paths:
      - 'services/master-service/**'
      - 'common/**'
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/reusable-java-build.yml
    with:
      service-name: master-service

  publish:
    if: github.event_name != 'pull_request'
    needs: test
    uses: ./.github/workflows/reusable-docker-publish.yml
    with:
      service-name: master-service

  deploy:
    if: github.event_name != 'pull_request'
    needs: publish
    uses: ./.github/workflows/reusable-deploy-oracle.yml
    with:
      service-name: master-service
      image-tag: ${{ needs.publish.outputs.image-tag }}
    secrets: inherit
