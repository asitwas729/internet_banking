# [임시 비활성화] 이 워크플로는 전체 주석 처리되어 동작하지 않습니다.
# 임의로 추가된 것으로 확인되어 비활성화함. 필요 시 아래 주석을 해제해 복원.
#
# name: Deploy auto-loan-review
#
# on:
#   push:
#     branches: [main]
#     paths:
#       - 'services/auto-loan-review/**'
#       - 'common/**'
#       - 'build.gradle'
#       - 'settings.gradle'
#       - '.github/workflows/deploy-auto-loan-review.yml'
#       - '.github/workflows/reusable-*.yml'
#   pull_request:
#     branches: [main]
#     paths:
#       - 'services/auto-loan-review/**'
#       - 'common/**'
#   workflow_dispatch:
#
# jobs:
#   test:
#     permissions:
#       contents: read
#     uses: ./.github/workflows/reusable-java-build.yml
#     with:
#       service-name: auto-loan-review
#
#   publish:
#     if: github.event_name != 'pull_request'
#     needs: test
#     permissions:
#       contents: read
#       packages: write
#     uses: ./.github/workflows/reusable-docker-publish.yml
#     with:
#       service-name: auto-loan-review
#
#   deploy:
#     if: github.event_name != 'pull_request'
#     needs: publish
#     permissions: {}
#     uses: ./.github/workflows/reusable-deploy-oracle.yml
#     with:
#       service-name: auto-loan-review
#       image-tag: ${{ needs.publish.outputs.image-tag }}
#     secrets: inherit
